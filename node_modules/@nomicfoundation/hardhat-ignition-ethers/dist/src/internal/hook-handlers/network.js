import { HardhatError } from "@nomicfoundation/hardhat-errors";
let EthersIgnitionHelperImpl;
class LazyEthersIgnitionHelper {
    type = "ethers";
    #hardhatConfig;
    #artifactsManager;
    #connection;
    #userInterruptions;
    #hooks;
    #config;
    #ethersIgnitionHelper;
    constructor(hardhatConfig, artifactsManager, connection, userInterruptions, hooks, config) {
        this.#hardhatConfig = hardhatConfig;
        this.#artifactsManager = artifactsManager;
        this.#connection = connection;
        this.#userInterruptions = userInterruptions;
        this.#hooks = hooks;
        this.#config = config;
    }
    async deploy(ignitionModule, options) {
        const ethersIgnitionHelper = await this.#getEthersIgnitionHelper();
        return await ethersIgnitionHelper.deploy(ignitionModule, options);
    }
    getResolvedConfig(perDeployConfig) {
        // Note: This duplicates the logic of the actual implementation because it's
        // a synchronous method, so we can't import the implementation.
        return {
            ...this.#config,
            ...perDeployConfig,
        };
    }
    async #getEthersIgnitionHelper() {
        // Note: `await import` must run BEFORE the instance cache check so that
        // concurrent callers share a single microtask-dedupe point — otherwise
        // each suspended caller re-enters the branch and constructs its own
        // impl, so callers end up holding different impl instances and state,
        // which can cause concurrency issues.
        if (EthersIgnitionHelperImpl === undefined) {
            ({ EthersIgnitionHelperImpl } = await import("../ethers-ignition-helper.js"));
        }
        if (this.#ethersIgnitionHelper === undefined) {
            this.#ethersIgnitionHelper = new EthersIgnitionHelperImpl(this.#hardhatConfig, this.#artifactsManager, this.#connection, this.#userInterruptions, this.#hooks, this.#config);
        }
        return this.#ethersIgnitionHelper;
    }
}
export default async () => {
    const handlers = {
        async newConnection(context, next) {
            const connection = await next(context);
            if (connection.ignition !== undefined) {
                throw new HardhatError(HardhatError.ERRORS.IGNITION.INTERNAL.ONLY_ONE_IGNITION_EXTENSION_PLUGIN_ALLOWED);
            }
            connection.ignition = new LazyEthersIgnitionHelper(context.config, context.artifacts, connection, context.interruptions, context.hooks, context.config.ignition);
            return connection;
        },
    };
    return handlers;
};
//# sourceMappingURL=network.js.map